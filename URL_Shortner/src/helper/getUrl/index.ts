import { redis } from "../../index.js";


const getUrlFromShortCode = (ShortCode: any) => {
    try {
        /// Check If the ShortCode is Present In Redis Cache, Else Hit the DB and Add the Data in Cache

    }
    catch (err) {

    }
}
